<?xml version="1.0" ?>
<odoo>
    <record id="view_order_tree" model="ir.ui.view">
        <field name="name">sale.order.tree</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="before">
                <field
                    name="qty_delivered_ratio_total"
                    widget="progressbar"
                    optional="show"
                />
                <field
                    name="qty_invoiced_ratio_total"
                    widget="progressbar"
                    optional="show"
                />
            </field>
        </field>
    </record>
    <record id="view_quotation_tree" model="ir.ui.view">
        <field name="name">sale.order.tree</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree" />
        <field name="arch" type="xml">
            <field name="invoice_status" position="before">
                <field
                    name="qty_delivered_ratio_total"
                    widget="progressbar"
                    optional="show"
                />
                <field
                    name="qty_invoiced_ratio_total"
                    widget="progressbar"
                    optional="show"
                />
            </field>
        </field>
    </record>

    <record id="view_order_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <field name="payment_term_id" position="after">
                <field name="qty_delivered_ratio_total" widget="progressbar" />
                <field name="qty_invoiced_ratio_total" widget="progressbar" />
            </field>
        </field>
    </record>

    <record id="view_sales_order_filter" model="ir.ui.view">
        <field name="name">sale.order.list.select</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <filter name="activities_upcoming_all" position="after">
                <separator />
                <filter
                    string="Delivered"
                    name="delivered_order"
                    domain="[('qty_delivered_ratio_total', '&gt;=', 100.0)]"
                />
                <filter
                    string="Partial Delivery"
                    name="partial_delivery_order"
                    domain="[('qty_delivered_ratio_total', '&gt;', 0.0),('qty_delivered_ratio_total', '&lt;', 100.0)]"
                />
                <filter
                    string="Not Delivered"
                    name="not_delivered_order"
                    domain="[('qty_delivered_ratio_total', '&lt;=', 0.0)]"
                />
            </filter>
        </field>
    </record>
</odoo>
